snapshots:
  - name: slots_snapshot
    relation: ref('slots_ephemeral')
    description:  markdown_string
    config:
      strategy: check
      unique_key: reservation
      check_cols: all
      hard_deletes: invalidate
